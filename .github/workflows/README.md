# GitHub workflows

## Docker Hub overview

The Docker publish workflow uses the repository `README.md` as the Docker Hub Overview page.

Keep Docker-facing usage instructions in the main README instead of creating a separate Docker Hub markdown file. This avoids duplicated docs drifting out of sync.
